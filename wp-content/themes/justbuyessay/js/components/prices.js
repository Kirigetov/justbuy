$(document).ready(function() {

    window.addEventListener("DOMContentLoaded", function() {
        var widget = $("[data-crm-widget='prices']"),
            format = widget.find('.prices-format-container'),
            formatTitle = format.find('.prices-format-left');
    });
});